# Test file for mixed format
# Simple format (must be before sections)
PORT: "3000"
ENV: "development"
DEBUG: "true"

# Section format
DATABASE_URL:
  value: "postgres://localhost/mydb"

SSL_CERT:
  file: "testdata/config_content.txt"

LOG_LEVEL:
  value: "info"

TOKEN: "hoge"
