# This should fail - nested profile is not allowed

INVALID_VAR:
  value: "default"
  profile:
    dev:
      value: "dev-value"
      # This nested profile should cause an error
      profile:
        sub:
          value: "nested-value"
