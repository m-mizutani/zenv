# Test command with refs feature

HOST:
  value: "prod-server"

PORT:
  value: "8080"

MESSAGE:
  command: ["echo", "Server: {{.HOST}}:{{.PORT}}"]
  refs: ["HOST", "PORT"]

SIMPLE_MESSAGE:
  command: ["echo", "Hello {{.NAME}}"]
  refs: ["NAME"]

NAME:
  value: "World"
