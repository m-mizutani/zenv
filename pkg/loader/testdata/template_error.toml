# Template with syntax error
[SYNTAX_ERROR]
template = "{{ .VAR_NAME"
refs = ["VAR_NAME"]

# Template without refs
[NO_REFS]
template = "This should fail"

# Template with refs but also other field
[MULTIPLE_TYPES]
value = "value"
template = "{{ .OTHER }}"
refs = ["OTHER"]