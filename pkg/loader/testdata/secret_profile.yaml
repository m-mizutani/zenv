DATABASE_URL:
  value: "postgres://user:pass@localhost/db"
  secret: true
  profile:
    dev: "postgres://dev:dev@localhost/devdb"

API_KEY:
  value: "prod-key"
  profile:
    dev:
      value: "dev-key"
      secret: true
